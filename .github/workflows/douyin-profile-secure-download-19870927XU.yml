name: Secure Douyin profile download 19870927XU

on:
  pull_request:
    branches: [main]
    paths:
      - '.github/douyin-profile-download-bootstrap-19870927XU'

permissions:
  contents: write

jobs:
  download:
    runs-on: ubuntu-latest
    timeout-minutes: 150
    steps:
      - name: Checkout temporary branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0

      - name: Publish ephemeral public key
        env:
          HEAD_REF: ${{ github.event.pull_request.head.ref }}
        run: |
          set -euo pipefail
          mkdir -p .douyin_download_exchange "$RUNNER_TEMP/douyin_download"
          openssl genpkey -algorithm RSA -pkeyopt rsa_keygen_bits:3072 -out "$RUNNER_TEMP/douyin_download/private.pem"
          openssl pkey -in "$RUNNER_TEMP/douyin_download/private.pem" -pubout -out .douyin_download_exchange/public_key.pem
          printf '%s\n' '${{ github.run_id }}' > .douyin_download_exchange/run_id.txt
          git rm -f .github/douyin-profile-download-bootstrap-19870927XU
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add .douyin_download_exchange
          git commit -m "Publish ephemeral Douyin download key"
          git push origin "HEAD:${HEAD_REF}"

      - name: Wait for encrypted login payload
        env:
          HEAD_REF: ${{ github.event.pull_request.head.ref }}
        run: |
          set -euo pipefail
          found=0
          for i in $(seq 1 120); do
            git fetch origin "$HEAD_REF" --quiet
            if git show "origin/${HEAD_REF}:.douyin_download_exchange/payload.json" > "$RUNNER_TEMP/douyin_download/payload.json" 2>/dev/null; then
              found=1
              break
            fi
            sleep 5
          done
          test "$found" = 1

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install cryptography requests playwright
          python -m playwright install --with-deps chromium

      - name: Decrypt authorized login request
        run: |
          mkdir -p .private
          python - <<'PY'
          import base64,json,os
          from pathlib import Path
          from cryptography.hazmat.primitives import hashes,serialization
          from cryptography.hazmat.primitives.asymmetric import padding
          from cryptography.hazmat.primitives.ciphers.aead import AESGCM
          root=Path(os.environ['RUNNER_TEMP'])/'douyin_download'
          obj=json.loads((root/'payload.json').read_text())
          key=serialization.load_pem_private_key((root/'private.pem').read_bytes(),password=None)
          aes=key.decrypt(base64.b64decode(obj['wrapped_key']),padding.OAEP(mgf=padding.MGF1(algorithm=hashes.SHA256()),algorithm=hashes.SHA256(),label=None))
          plain=AESGCM(aes).decrypt(base64.b64decode(obj['nonce']),base64.b64decode(obj['ciphertext']),b'douyin-download-19870927XU')
          Path('.private/douyin_login_curl.txt').write_bytes(plain)
          PY

      - name: Refresh profile enumeration
        run: python tools/douyin_profile_enumerate.py

      - name: Download all HD no-watermark videos
        id: download_videos
        continue-on-error: true
        run: python tools/douyin_profile_download.py

      - name: Remove plaintext credentials
        if: always()
        run: rm -rf .private "$RUNNER_TEMP/douyin_download/private.pem" "$RUNNER_TEMP/douyin_download/payload.json"

      - name: Upload video archive
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: douyin-profile-videos-19870927XU
          path: |
            lyx520_export/
            douyin_profile_enum/枚举摘要.json
            douyin_profile_enum/视频下载计划.json
          if-no-files-found: warn
          retention-days: 3
